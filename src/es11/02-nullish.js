const anotherNumber = null;
//el operador ?? permite comprobar si el valor de la variable es null, 
//si es el caso entones le asignamos el valor 5.
const validate = anotherNumber ?? 5;
console.log(validate);